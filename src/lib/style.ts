/**
 * OpenFreeMap provides free, no-API-key vector tiles + ready-made styles.
 * https://openfreemap.org
 *
 * "liberty" includes 3D building geometry (OpenMapTiles schema) which we
 * extrude in the city view.
 */
export const STYLE_URL = 'https://tiles.openfreemap.org/styles/liberty';

/** Camera used while riding around a city, third-person behind the avatar. */
export const RIDE_ZOOM = 18.5;
export const RIDE_PITCH = 65;
